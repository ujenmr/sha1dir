sudo: required
language: go
go:
- master
services:
- docker
before_install:
- docker-compose pull
script:
- docker-compose up
deploy:
  provider: releases
  api_key:
    secure: "$api_key"
  file: bin/sha1dir.linux
  on:
    repo: ujenmr/sha1dir
